import React from 'react';
import {Link, Outlet} from 'react-router-dom'; // Импортируйте Outlet

const App = () => {
    return (
        <div>
            <h2>Содержимое страницы</h2>
        </div>
    );
};

export default App;